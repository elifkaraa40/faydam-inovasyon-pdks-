name: ilk_mobil_uygulamam
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  mobile_scanner: ^5.1.1
  uuid: ^4.3.3
  intl: ^0.20.2
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# Projedeki TEK VE ANA flutter bloğu burasıdır.
# Yukarıda başka bir yerde "flutter:" veya "uses-material-design" kalmadığından emin ol.
flutter:
  uses-material-design: true

  # Klasördeki fiziksel dosyanız .jpg olduğu için burayı .jpg olarak tanımladık
  assets:
    - assets/faydam_logo.jpg
    - assets/faydam_logo_wide.png
